import { Component } from '@angular/core';

// Stub component — hiện tại form được build trực tiếp trong HomeComponent
// để giữ state management đơn giản. Component này giữ lại trong structure
// để dễ refactor sau nếu cần tách form ra riêng.
@Component({
  selector: 'app-loan-input-form',
  template: ''
})
export class LoanInputFormComponent {}
